jdk:
  - openjdk17

# JitPack publishes the library jar. Tests are dev-only (DevCli is a manual
# CLI helper) so we still skip executing them, but compileTestKotlin must
# pass so the dev CLI doesn't silently rot against new koog releases.
install:
  - ./gradlew publishToMavenLocal -x test
