jdk:
  - openjdk17

# JitPack publishes the library jar; tests are dev-only (DevCli is a manual
# CLI helper). Skip test compile + run on koog version bumps until DevCli
# is migrated to the new koog 0.8 DSL.
install:
  - ./gradlew publishToMavenLocal -x test -x compileTestKotlin
